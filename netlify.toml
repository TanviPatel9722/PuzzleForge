[build]
  publish = "backend/static"
  command = "echo no-build"

# Proxy API calls to your deployed backend (replace this URL)
[[redirects]]
  from = "/api/*"
  to = "https://puzzleforge.onrender.com"
  status = 200
  force = true